#spring.application.name=web-store-task-2
spring:

  flyway:
    enabled: true
  application:
    name: web-store-task-2
  datasource:
    url: "jdbc:postgresql://localhost:5432/postgres?currentSchema=test"
    username: "postgres"
    password: "1"
    driver-class-name: org.postgresql.Driver
server:
  servlet:
    context-path: /api
  port: 8081
